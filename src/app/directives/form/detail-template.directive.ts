import {Directive, TemplateRef} from '@angular/core';

@Directive({
  selector: '[appDetailDirective]'
})
export class DetailTemplateDirective {
  // constructor( private tpl: TemplateRef<any> ) {}
}
